Steps to Run :

I > Using IDE :
 	1. Import the project to ide 
 	2. Run TestNg File

II > Using CMD :
	 1. Navigate to the folder where pom is present using command prompt
	 2. In command prompt type "mvn clean test"(Make sure your having maven)